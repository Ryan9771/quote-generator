import getStyles from "../../Style";

function TextBox() {
  return (
    <div className={getStyles(styles, "ctn")}>
      <div></div>
    </div>
  );
}

const styles = {
  ctn: [],
};

export default TextBox;
